[package]
name = "forge-drm-sys"
version = "0.1.0"
edition = "2021"
description = "FFI bindings for libliftoff and libdisplay-info"

[build-dependencies]
pkg-config = "0.3"

[dependencies]
libc = "0.2"
